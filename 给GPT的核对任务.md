# 给 GPT 的核对任务：PoE2 锐眼冰射双阶段天赋树

> 你是被委托做**独立核对**的一方。请用你实际能跑的工具去查，不要凭印象回答。
> 如果你无法访问文件或网络，请**如实说「没法验证」**，不要推测。

---

## 一、背景

国服《流放之路：降临》0.5.5，BD 是 B 站 UP 主 **网瘾老年人lowK** 的「锐眼冰射狙击」。
他本人有两个阶段：

- **第一天**：非暴击开荒（1~70 级左右），练到能刷异界 T15
- **第二天**：转暴击后的后期（70 级起）

我已经拿到他**第二天的角色导出**（poe.ninja 的 `.build`，93 级，151 个天赋节点），
并且从他这棵树上**推**了一份第一天的开荒树（去掉暴击/眩晕系）。
现在需要独立核对下面三件事。

## 二、可直接获取的文件

公开仓库（GitHub Pages，可直接下载）：

- `https://harryghost.github.io/poe2-guide/POE2-Author-Tree-D2.build` —— 第二天（他原版）
- `https://harryghost.github.io/poe2-guide/POE2-Author-Tree-D1.build` —— 第一天（我推的）
- `https://harryghost.github.io/poe2-guide/` —— 攻略站点（含 5 张天赋树原图）

Google 搜「poe2-guide HarryGhost」也能找到，或直接猜仓库：
`https://github.com/HarryGhost/poe2-guide`

## 三、需要核对的三件事（按重要性）

### ① D2 是不是「原版 100%」+ 点数是否合理

已经查到的：
- D2 的 151 个 passive ID，**全部**能在 PoB2 的 0.5 天赋树数据
  （`src/TreeData/0_5/tree.json`，GitHub: `PathOfBuildingCommunity/PathOfBuilding-PoE2`）
  的 `stringId` 集合里找到 → ID 层面是合法的
- D2 用全图最短路径校验，是 **1 个连通块（140 节点）+ 1 个涂油**
  （`DeliriumAnoint_ThaumaturgicGenerator`，涂油本来就不靠树连接）

**需要你核对的疑点（我没解决）**：
他角色是 **93 级**，但 D2 有 **141 个普通天赋节点**。
按「每级 1 点 + 任务/间章 24 点」估算，93 级应该只有 **117 点**，
差了 **24 点**。

我的猜测：这 24 点是**双武器天赋（两套武器各有点数）**，poe.ninja 导出时把两套都算进去了。

**请回答**：
1. 0.5.5 到底有没有「双武器天赋」？总共给多少点？（查官方 patch notes 或 PoB2 源码）
2. 如果有：D2 的 141 个节点里，哪些属于「第二套武器」？（这需要节点上的 `weapon_set` 字段或按树上的分布判断）
3. 导入游戏后会不会显示「点数不够」？如果会，应该怎么修（比如只保留第一套）？

### ② D1（我推的开荒树）点数是否合理

我的做法：取 D2 的 151 个节点，**剔除**：
- 所有 `criticals*` 前缀节点（暴击系）
- 所有 `daze*`（眩晕）、`shock*`（感电）、`DeliriumAnoint`（涂油）
- 属性文本里含 `critical` 的大点

**保留**：冰霜穿透 / 冰冻积累 / 投射物 / 弓（含「额外箭矢几率」）/ 属性点 / 珠宝孔 /
闪避+护盾 / 移动速度 / 印记 / 敏捷 等等。

结果：**D1 = 104 个天赋节点**，用全图最短路径校验是 **1 个完整连通块**。

**需要你核对的疑点**：
- 104 点对应 **80 级**（104-24）。但「第一天」按他的视频是 70 级左右打完 T15，
  应该是 **90~95 点**。**我的 D1 可能多了 10~15 点。**
- 他视频里说：「70 级左右就可以转暴击」「转暴击需要留 100 万金币洗点」

**请回答**：
1. 第一天（70~75 级）合理的点数应该是多少？
2. 从 D1 的 104 点里，应该再删哪些节点才能降到 ~90 点，同时保持**技能命中/连招可用**
   （他第一天靠的是：闪电箭矢/引雷针过渡 → 31 级冰霜射击 → 狙击 → 冰冻印记 + 冻结齐射 + 弹幕）？
3. 有没有可能我剔多了（比如某些被剔的节点第一天就该点）？

### ③ 图片能不能提取（**这一条请不要投入大精力，我已有定量结论**）

他文档里有 5 张天赋树截图（1500px 宽，繁体界面）。我做过定量测试，结论是**不可行**：

| 我的尝试 | 结果 |
|---|---|
| 检测「发光的大节点」（已点的关键点） | 每图稳定检出 10~11 个，5 图合计约 40 个 |
| 推算真实节点坐标（`组中心 + 轨道半径×(sinθ,cosθ)`） | ✅ 4476 节点 → 4464 个不同坐标 |
| 由树的坐标算节点间距 | 应约 **14 像素** |
| 实测检出的「暗点」最近邻间距 | **140~215 像素**（差 10 倍）→ 检出的是背景噪点 |
| 用 D2 的 32 个真实大点做锚去拟合图 13/图 23 | 每图最多对上 **4/10**（放 32 个候选里随机也能中 ~3 个）→ 噪声水平 |

`/tmp/tree_nodes_real.json` 是修好后的坐标表（本机路径，格式 `{数值ID:[x,y,stringId,name,isNotable]}`）。
真实节点坐标公式（这是个有价值的发现，可以直接复用）：
```
节点坐标 = group中心(group.x, group.y) + 轨道半径 × (sinθ, cosθ)
θ = constants.orbitAnglesByOrbit[orbit][orbitIndex]
orbitRadii = [0, 82, 162, 335, 493, 662, 846, 251, 1080, 1322]
```

**请只回答一句**：你认同「这 5 张图不足以还原 120 点树」吗？还是你觉得有别的方法我没试？

## 四、你可以用的数据源

1. **PoB2 仓库**（权威）：
   - `src/TreeData/0_5/tree.json` —— 节点/连接/坐标/大点标记
   - `src/Modules/BuildExportPoE2.lua` —— `.build` 的官方导出逻辑（能看出游戏要什么字段）
   - `src/Data/InventorySlots.lua` —— 装备槽 ID
2. **GGG 官方文档**：`https://www.pathofexile.com/developer/docs/game`
   （`.build` 格式：`passives` 用 PassiveSkills 的 string id，例如 `strength89`）
3. **真实可用的 .build 样本**（用来对照格式）：
   `https://github.com/chesler410/poe2-build-forge/tree/main/fixtures`
   （里面有玩家导出的文件，`passives` 都有 180+ 个对象，带 `level_interval`）
4. **POE2 的中文资料站**：`https://poe2db.tw/cn/`

## 五、我踩过的坑（别重踩）

1. **别用「组中心坐标」做几何匹配** —— `tree.json` 里 `groups[id].x/y` 是组中心，
   21 个节点会挤在同一个点上。必须按上面那个公式算真实坐标。
2. **「树→像素」和「像素→树」不要搞反** —— 反了会退化成恒等变换，全是假阳性。
3. **验证方向要双向** —— 只检查「亮点能否找到节点」不够，还要检查「节点投到像素能否落在亮点上」。
4. **连通性校验别从职业起点节点出发** —— 起点（`ranger596`）是没有 `stringId` 的，
   不在节点集合里，从它 BFS 会返回「可达 0」。正确做法：先算已选节点的连通块，
   再用**全图**最短路径把散块接到主块。
5. **点数预算要按等级核对** —— 每级 1 点 + 任务/间章 24 点。这是发现 D2 那 24 点疑点的方法。

## 六、期望的输出格式

请按这个结构回复，每条都注明**依据出处**（哪个文件/哪条 patch note）：

```
① D2 双武器点数疑点
   - 0.5.5 是否有双武器天赋：是/否，依据：
   - 24 点差额的解释：
   - D2 里哪些节点属于第二套：
   - 导入是否会「点数不够」：
   - 建议的修法：

② D1 点数与节点取舍
   - 第一天合理点数：
   - 建议从 D1 再删的节点（列出 stringId 或节点名）：
   - 是否有被误删的节点（列出）：
   - 修订后的节点数：

③ 图片提取
   - 认同 / 不认同（一句话）+ 理由

④ 无法验证的部分（如实列出）
```

## 七、协作方式

我这边能做的事（你说要我改我就改）：
- 重新生成 `.build`（我有完整的树数据、宝石数据、可复现脚本）
- 部署到网站、回读验证
- 你只需要给出**节点级别的判断**（哪些留、哪些删、哪些加），我来落地
