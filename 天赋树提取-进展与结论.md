# 天赋树提取：进展与结论

## 关键发现（之前的失败原因找到了）

我之前用的坐标表 `/tmp/tree_nodes_xy.json` **是错的**——它给的是天赋树「组中心」坐标，
不是节点的真实位置。所以 4476 个节点里只有 1362 个不同坐标（21 个节点挤在同一个点上），
拿它做几何匹配必然乱配。

真实的节点坐标要按公式算：

```
节点真实坐标 = 组中心(group.x, group.y) + 轨道半径 × (sin θ, cos θ)
θ = orbitAnglesByOrbit[orbit][orbitIndex]
半径表 orbitRadii = [0, 82, 162, 335, 493, 662, 846, 251, 1080, 1322]
```

按这个公式重算后：**4476 个节点 → 4464 个不同坐标**（只剩 12 组重复，都是 mastery 占位点）。
新的坐标表已生成：`/tmp/tree_nodes_real.json`（格式 `{数值ID: [x, y, stringId, name, isNotable]}`）

## 现在的状态

匹配程序 `/tmp/v2/geom3.py` 正在用**正确的坐标表**跑（后台运行中）：

- 用图 13 的 10 个亮点，取距离最远的 8 组点对做锚
- 尺度限制在 0.06–0.15（因为亮点相邻间距 75–525 像素）
- 2 对点解相似变换 → 把 4464 个树节点投到像素 → 统计有多少落在亮点 6 像素内
- 命中 ≥7 的解才留下

进度：每 40 秒约查 8.4 万个候选，**每约 50 个候选就有 1 个「命中」**——
这个命中率高得可疑，说明里面绝大多数是假阳性（几何巧合），
需要靠**叠图目视确认**挑出唯一正确的那一个。

## 判据（怎么知道哪个解是对的）

把候选变换投回截图（工具已备好：`/tmp/overlay`，用法见下），正确的解应该满足：

1. 10 个亮点全部命中，误差 **< 3 像素**
2. 命中的节点**名字**要跟这套 BD 对得上（冰霜/投射物/闪避/暴击/敏捷），
   出现「火焰/召唤物/图腾/格挡」这类就是错的
3. 参数落在合理区间：尺度 ≈ 0.06–0.12，平移在画面内

叠图命令：
```
/tmp/overlay images/13.jpg /tmp/check.png "x1,y1,x2,y2,..." <尺度> <tx> <ty> <flip>
```
（坐标序列传树节点坐标，工具会画红圈）

## 拿到正确变换之后还剩两步

1. **补小节点**：10 个亮点是「大节点」，整棵树有 120+ 个节点。
   需要沿 `tree05.json` 里每个节点的 `connections` 在已识别的大节点之间找最短路径，
   把路径上的小节点补进去 → 这才是作者真正点过的连通树。
2. **核对点数**：90 级约 113 点，95 级约 118 点。对不上说明还漏了节点。

## 老实说的时间预估

- 匹配跑完：按当前速度约 1–2 小时（纯 Python 无 numpy，是主要瓶颈）
- 从候选里挑出正确解 + 叠图确认：20–40 分钟（需要反复迭代）
- 补路径 + 点数核对 + 写进 .build + 部署：20 分钟

**总计 2–3 小时**，而且最后**仍有可能因为「小节点认不全」而拼不出完整的 120 点**——
到那一步只能退回到「大节点 + 最短路径」的近似树，不是他 100% 原版。

## 更快的路（对比）

| 方式 | 耗时 | 准确度 |
|---|---|---|
| PoB 导出（用户点一次 Export） | 5 分钟 | 100% |
| poe.ninja 角色页的 Build Planner 按钮 | 30 秒 | 100% |
| 本文件的几何提取 | 2–3 小时 | 大概率不完整 |

所以：**如果只要结果，走 PoB / poe.ninja；如果一定要从图里挖，就等这个程序跑完再叠图确认。**


---

# 追加：进展、以及我这 30 多分钟到底干了什么（老实交代）

## 你的两个问题，都是对的

**1. 「为什么看 4000 多个技能点」——我犯了个大错。**

天赋树全部 4476 个节点，但**从锐眼起点出发能到达的只有 177 个**（距离起点 2500 单位内）。
我一开始把整棵树当搜索池，等于把搜索空间放大了 25 倍。正确做法是先算「可达半径」，
只在 177 个节点里搜。这一条我走了弯路。

**2. 「30 多分钟干了什么」——四件事，其中两件是白干的。**

| 时间 | 做了什么 | 结果 |
|---|---|---|
| 约 5 分钟 | 在截图里找「发光的大节点」 | ✅ 每图稳定检出 10 个 |
| 约 10 分钟 | 用错误的坐标表做几何匹配 | ❌ 白干（坐标表是组中心，不是真实位置） |
| 约 15 分钟 | 按 orbit 公式重算真实坐标，重跑匹配 | ⚠️ 坐标表修对了，但搜索池仍是 4476 个，跑不完 |
| 约 5 分钟 | 想用「暗的小圆点」当网格对齐 | ❌ 失败：检出的是背景噪点（间距与节点不符），不是节点 |

## 现在的确切状态

已经拿到的确定结论：

1. **真实坐标公式**（这是个有价值的发现，之前谁都可能是踩在这里）
   `节点坐标 = 组中心 + 轨道半径 × (sinθ, cosθ)`，半径表 `orbitRadii`。
   重算后 4476 个节点 → 4464 个不同坐标。坐标表：`/tmp/tree_nodes_real.json`
2. **锐眼可达池 = 177 个节点**（起点 50459 半径 2500 内）
3. **每张图能可靠检出的「已点大节点」= 10 个**（图13/15/23），
   而整棵树有 110~120 个已点节点 → **缺口就是那些小节点，图上只有 3~4 像素**
4. 5 张图理论上覆盖同一棵树的不同区段：13（10 个）+14（7 个）+23（10 个）+15（10 个）+16（3 个）
   = **去重后最多 40 个大节点**，仍不足 120 个

## 为什么这条路走不通（技术上的硬墙）

游戏天赋树的已点节点里，**「过路小点」占大多数**（加 10% 伤害、+5 属性那种）。
这类点在截图里只占 **3~4 个像素**，而画面的背景是星云状噪点（我抽样过：暗区像素值
`(18,18,16)`、`(8,11,16)` 这种），两者在数值上无法区分。

**所以：能从这些图里认出来的只有「大节点」约 40 个，够不到 120 个。**
不是算法问题，是信息量问题——他给的图就是这么大（1500px 宽，还被压缩过）。

## 两条真正的出路

| 方式 | 需要谁做什么 | 耗时 | 准确度 |
|---|---|---|---|
| **PoB 导出** | 你点 Export（或让他点） | 5 分钟 | 100% |
| **poe.ninja 的 Build Planner 按钮** | 你点一下下载 | 30 秒 | 100% |
| 我从图里挖 | 我 | 2~3 小时 | **最多 40 个节点，拼不出完整树** |

我建议走第 2 条。你只要把下载到的 `.build` 丢给我，本机流水线
`/tmp/v2/ingest_build.py` 会做完校验、改名、转换、部署，然后给你确认报告。
