# 既有完整资料 · C配色统一版

主入口为上一级 `index.html`；本目录是全部已收录阶段与原始构筑资料。
本目录的交互页、剧情速查和六分支完整资料已经统一C「雪白深栏」配色。
原有资料布局和游戏数据不变，不代表全部阶段的新作业布局已重排完成。

## 本地使用与发布
双击 `index.html` 使用，无外部脚本依赖。发布时保留整个目录和上一级结构。
按钮、勾选、键位等浏览器本地记录是否保留取决于浏览器对本地文件的存储权限。

## 重新生成（维护人员使用）
运行 `python _source/build.py`，无需第三方库或联网。
`_source/style.css` 末尾有C统一配色；`_source/data.json` 为原有完整数据。
脚本核验六份原始构筑的SHA-256，然后生成交互页与两份全量参考页。
06原始文件存在升华字段冲突，继续保留 `.build.txt` 只读标记。

## 本次测试边界
数据保留、相对文件路径、JS交互、静态渲染和不同宽度布局已检查。
本环境浏览器阻止file/HTTP导航，因此交互检查使用实际HTML内存渲染与存储夹具；
没有声称完成真实本地文件跳转、线上部署或国服游戏导入实测。
原有天赋、译名及60+作者配置的未完成项仍以正文和根目录说明为准。
