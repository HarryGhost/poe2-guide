# 关于那 5 张天赋树截图：能看清什么、不能看清什么

## 结论

这 5 张图**不足以还原作者第一天的天赋树**。下面是我实测出来的边界，量化过：

| 项目 | 实测结果 |
|---|---|
| 图的分辨率 | 1500px 宽（文档里原图最大 2.6MB，被压缩过） |
| 天赋树整体跨度 | 约 25,000 单位 |
| 换算 | 约 **16 单位 / 像素** |
| 一个节点的图标尺寸 | 约 60 单位 → 图上只有 **3~4 像素** |
| 我能稳定认出的 | 只有**发光的大点**，每图 3~10 个（5 图合计约 40 个，有重复） |
| 我需要还原的 | 120+ 个已点节点（其中大多数是过路小点） |
| 已点的「大点」在图上 | 有光晕，能看到一团亮斑；**但看不出它是什么节点**（名字/图标细节在图里无法分辨） |
| 过路小点 | 3~4 像素、和背景噪点同色 → **认不出，也数不出** |

所以：**图上能"看见"一些亮点，但看不见"点的是什么"。** 这不是分辨率不够到看不见，而是不够到"分辨内容"。

## 我试过的、并且失败的方法（别再重复）

1. 亮点检测（按亮度+饱和度）→ 检出的 100~160 个"圆点"里大半是背景星云，不是节点
2. 用树的真实坐标去拟合 → 用作者**逐节点导出的那棵树**去对，每图最多也只覆盖 **5/10** 个亮点
3. 用「未点的小点网格」对齐 → 检出的点间距 140~215 像素，而真实节点间距应约 14 像素，差 10 倍

**结论：这些截图既不能"读"，也不能"验"。** 

## 那什么能验证？（三条真路）

| 方式 | 需要谁做什么 | 可靠性 |
|---|---|---|
| **导出作者第一天的角色**（如果那角色还在） | 用户去 poe.ninja 看有没有另一个 character，导出 `.build` | **100%** |
| **游戏内天赋树的分享/导出功能** | 用户让作者用游戏里的分享功能出一份（而不是截图） | 高 |
| **作者自己在视频里逐点点天赋的那一段** | 用户看视频，把能看清的**大点名字**（中文名）报给我 | 中（只能验大点） |

## 我这边已交付的状态

- `POE2-Author-Tree-D2.build`：作者本人导出，150 条（140 普通 + 8 付费升华 + 2 免费结构）→ **可靠**
- `POE2-Author-Tree-D1.build`：93 点，是**从他第二天那棵树推的参考版**（剔暴击/眩晕/感电、补命中与攻速）→ **不是作者第一天的原图，无法验证**
- 站点上「天赋加点」页放的就是这 5 张截图

## 给 GPT 的一句话

**不要尝试从这 5 张截图里读节点。** 需要数据就用上面三个真路之一；拿不到就按「参考配置」发布，并标注清楚它不是作者原图。
