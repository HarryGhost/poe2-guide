# 给 GPT：天赋树还是不对，请重新出一份

## 一、现状（我之前给你的两份）

| 文件 | 内容 | 状态 |
|---|---|---|
| `POE2-Author-Tree-D2.build` | 作者本人 poe.ninja 导出，**151 个 passive** | 节点 ID 逐条核对过，set 相等；但**点数疑点未解决** |
| `POE2-Author-Tree-D1.build` | 我按你上次方案改的，**93 个节点**（104 − 13 + 2） | 你给的删/加清单我全部核对通过并落地了 |

下载：
- https://harryghost.github.io/poe2-guide/POE2-Author-Tree-D2.build
- https://harryghost.github.io/poe2-guide/POE2-Author-Tree-D1.build

**用户反馈：天赋点还是不对。** 我这边没有能自动校验「对不对」的手段（无法运行游戏），
所以需要你给一份**更可验证**的结论。

## 二、两个我判断不了的关键问题（请优先回答）

### Q1：D2 的 151 个 passive 里，到底有几个是真正点着的天赋点？

已知矛盾：
- 角色是 **93 级**
- D2 有 **141 个普通天赋节点** + 10 个升华节点
- 按「(等级−1) + 任务/间章 24 点」→ 93 级可用 **116 点**（(93−1)+24）

141 比 116 多 **25 点**。可能的解释（请你判断哪个对）：
1. **双武器天赋**：poe.ninja 把两套武器组的点数导在一起了
2. poe.ninja 的 `.build` 里 `passives` 还包含了「副天赋」那一棵（作者视频里提过双天赋）
3. 93 级其实是别的东西（比如角色实际上限不是 93，或者任务点不止 24）

**请回答**：
- 到底哪个解释对？依据是什么（patch notes / PoB2 源码 / 实际导出样本）？
- 如果导入游戏会「点数不够」，游戏会怎么处理？（报错拒绝？还是只点亮一部分？）
- 要修成「能正确导入」的版本，应该删掉哪些节点（给 string id）？

### Q2：第一天（D1）到底应该多少点、点哪些？

我给的目标是 70 级 = 93 点，你上次也确认了 93 点。用户仍说不对。

**请直接给一份「你认为正确的第一天节点表」**，而不是只给增删差集 —— 这样我能整棵替换。
要求：

1. 每个节点给 **string id**（形如 `cold_penetration24`），不要只给中文名
2. 说明是按**几级**、**多少点**设计的
3. 保证从锐眼起点连通（只用已选节点即可连通）
4. 说明你依据什么判断「第一天该点这个」：
   - 作者视频原话？（是哪一句）
   - 技能联动？（比如第一天用什么技能）
   - 还是 PoB 实算的 DPS/命中？

## 三、核对时请注意（我踩过的坑）

1. **按关键字前缀过滤会误判机制**：`passive_keystone_resonance` 名字里没有 crit，
   但它会把狂怒球变暴击球、破坏第一天的供球链 —— 你上次抓到了这条，我认。
2. **`tree.json` 的 `groups[].x/y` 是组中心坐标，不是节点位置**。真实坐标：
   ```
   节点坐标 = 组中心 + 轨道半径 × (sinθ, cosθ)
   θ = constants.orbitAnglesByOrbit[orbit][orbitIndex]
   orbitRadii = [0, 82, 162, 335, 493, 662, 846, 251, 1080, 1322]
   ```
3. **连通性校验不要从职业起点 BFS**：`ranger596` 这个起点节点**没有 stringId**，
   不在节点集合里，从它 BFS 会返回「可达 0」。正确做法：已选节点做诱导子图找连通块，
   再用**全图**最短路径把散块接起来。
4. 全部 4903 个节点的可用信息：`src/TreeData/0_5/tree.json`
   （`stringId` / `connections` / `orbit` / `orbitIndex` / `group` / `isNotable` / `stats`）

## 四、输出格式（照这个回）

```
① Q1 结论
   - 151 个 passive 里真正点着的点数：
   - 25 点差额的解释与依据：
   - 导入游戏的行为（报错 / 部分点亮 / 正常）：
   - 要删哪些节点（string id）：
   - 修好后的 D2 节点数：

② Q2：第一天（D1）的完整节点表
   - 目标：___ 级 / ___ 点
   - 判断依据（写明是原话、联动还是实算）：
   - 节点 string id 全表（一行一个，或逗号分隔）：
   - 与「作者 D2」的差异（哪些是第二天才加的）：

③ 你认为我哪里又错了
```

我这边有完整的树数据（4903 节点）、宝石数据（967 条）和可复现脚本，
你给 string id 我就能生成 `.build`、做合法性/连通性/点数校验并部署。
