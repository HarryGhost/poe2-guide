# 学习笔记（等 GPT 的回复后补充）

> 用途：把 GPT 的方案消化成可复用、可验证的规则，以后 Harry 自己维护这个网站。
> 现在只有「待验证项」，等对方回复后逐条补上结论。

## 一、网站优化（等 GPT 给方案）

等他回复后，我要逐条验证再采纳，不照抄：

| 他给的方案 | 我要验证什么 | 状态 |
|---|---|---|
| 导航改成 X 个主入口 | 窄屏（375px）下是否溢出、点得到 | 待验证 |
| 移动端 CSS 补丁 | 用真实宽度渲染确认；表格/图片网格不挤压 | 待验证 |
| 图片压缩方案 | 压缩后肉眼是否可读（字要能看清）、总量降多少 | 待验证 |
| 内容去重清单 | 逐一确认不是我漏了什么（有内容的别删） | 待验证 |

**取舍原则**：他给的具体改动（CSS 片段、导航项、尺寸参数）我落地；
但任何「改攻略文字」的提议一律拒绝 —— 文字照抄原作者，改错就是误导新手。

## 二、BD 核对方法（重点：他是怎么把 D1 搞定的）

我对 D1 目前的自我评估，以及需要他回答的关键点：

**已确认没问题**：
- D2 的 151 个节点：ID 逐条对过 PoB2 的树数据，set 完全相等
- 两个文件都是合法连通分配（用「已选节点诱导子图 + 全图最短路径」验的，不是猜的）

**我自己发现、还没解决的疑点**：
1. D2 是 93 级却有 141 个普通天赋节点 —— 按「每级 1 点 + 任务 24 点」应该是 117 点，
   差的 24 点疑似「双武器天赋」
2. D1 现在 104 点 ≈ 80 级，但第一天按视频是 70 级左右 → 可能多了 10~15 点

**如果他能给出 D1 的修订，我要重点看他用了什么方法**：
- □ 他是否用了双武器点数机制（如果是，怎么从 .build 里区分的？看 `weapon_set` 字段？）
- □ 他的点数预算公式是什么（每级几点？任务给几点？和我用的 1+24 差在哪）
- □ 他是怎么定「哪些节点第一天必须点」的判据
      （按技能联动？按伤害公式？还是按 PoB 的实际 DPS 计算？）
- □ 他有没有自己重建一棵树（还是只在我的 104 点上改）
- □ 他判断「连通」的方法（是不是也踩过「从职业起点 BFS」的坑）

**学到的判据要写进这里**：————————————————

## 三、可复用的技术资产（现有）

- 真实节点坐标公式（这次最大的发现）：
  `节点坐标 = 组中心 + 轨道半径 × (sinθ, cosθ)`，`θ = orbitAnglesByOrbit[orbit][orbitIndex]`
  `orbitRadii = [0, 82, 162, 335, 493, 662, 846, 251, 1080, 1322]`
  → 用组中心坐标做几何匹配会乱配（21 个节点挤一个点）
- 合法性校验：已选节点的连通块 + 全图最短路径补桥（**不要**从职业起点 `ranger596` BFS，它没有 stringId）
- 点数预算核对：等级 + 任务点，能暴露「双武器」这类隐性点数
- `.build` 生成流水线：`/tmp/v2/` 下的脚本（ingest_build / make_d1 / verify_legal / build.py）

## 四、更新网站的标准流程（以后按这个走）

1. 改 `/tmp/v2/` 里的生成脚本（不手改 index.html）
2. `python3 build.py` 重新生成
3. 校验：HTML 标签平衡、锚点无死链、图片无 404
4. `git commit && git push` → 等 GitHub Pages 部署
5. 打开线上 URL 回读确认（字符数、关键文案、文件 HTTP 200）

## 五、会话太长导致打字卡（已发现）

**现象**：跟 Harry 对话时输入框卡；关掉对话就正常 → 是会话体积问题，不是电脑问题。

**原因**：
- 本工作区的会话记录已经到 **6~8MB（压缩后）**，解压约 30~60MB JSON
- 每轮对话浏览器都要重渲染整段历史；上下文也随轮次线性增长

**我的责任（要改）**：
- 一条回复里塞了太多东西（十几行到上百行的表格、60 行的代码块、大段中文）。这些全会进 DOM 和上下文。
- 以后规则：
  1. 回复**短**，先给结论，细节放文件
  2. 长内容一律写成文件 / 网页，聊天里只放**链接 + 两三行摘要**，不再整段贴在聊天里
  3. 大任务拆成多次小交互，别一次吐几千字

**建议用户的**：一个话题做完就开新会话（关掉重开），长会话只用于同一件事。


## 六、重要：网站的生成方式已经变了（2026-09-13）

优化前：`index.html` 由 `/tmp/v2/build.py` 组装生成。
**优化后**：`index.html` 是**直接编辑的最终产物**，里面包含 GPT 方案的三批改动
（4 主入口导航、hash 路由、移动端 CSS、宽表横滑、装备页折叠、图片两档预览、
配图页去重、.note 类）。

⚠️ **所以：再跑 `build.py` 会把优化全部覆盖掉。**
以后的正确做法：
1. 直接改 `index.html`（或写一个「只做本次改动」的脚本，像 `/tmp/v2/apply_*.py` 那样）
2. 改完必须校验：标签平衡、锚点无死链、图片无 404（含 `images/thumb/` 下的缩略图）
3. 参考 `/tmp/v2/index_optimized_final.html`（本次优化后的快照）

图片缩略图：`images/thumb/*-480.jpg`、`*-960.jpg`，由 `sips` 生成（这台机器没有 cwebp）。
新增图片时要一起生成两档，并给 `<img>` 补 `width/height/decoding`。
