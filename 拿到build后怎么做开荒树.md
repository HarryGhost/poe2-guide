# 拿到他的 .build 之后，我怎么把「开荒树」也做出来

## 前提：他给的那份是**第二天暴击**的树，不是开荒树

这一点用户已经确认。所以要做两份：

| 文件 | 天赋树来源 | 准确度 |
|---|---|---|
| `POE2-Author-Tree-D2.build` | .build 里原样的树，一个节点不动 | **100% 他的原版** |
| `POE2-Author-Tree-D1.build` | 从上面那棵树**推**出开荒树 | 高（见下面的依据），但不是他原版 |

## 「开荒树是最终树的子集」——这个判断的依据

这套 BD 的路线是：**第一章~40 级走非暴击冰射** → **70 级左右转暴击**（他原话：
「70 级左右就可以转暴击」，转的时候要留 100 万金币洗点）。转暴击是**加点方向变了**，
不是把树推翻重练。所以：

- 大节点（冰霜/投射物/闪避/敏捷/生命）两边都在
- 第二天多出来的是**暴击圈**（他讲第二天天赋：「这里也可以加个暴击对吧，25%……
  这里有个暴击几率 12，再加这里就是 37%」）和**偏斜圈**（「把加点挪一部分来这个偏斜圈，
  这里 4 点，这里 5 点，总共 9 点」）

## 具体怎么做（拿到文件后执行）

1. **体检**
   用 `/tmp/v2/ingest_build.py` 读出来：`passives` 节点数、每个节点 ID 是否在
   `/tmp/tree_nodes_real.json` 的 stringId 集合里、`ascendancy` 是否为 `Ranger1`

2. **分拣节点**（对每个已点节点判断它属不属于开荒树）
   - 保留：投射物 / 弓 / 冰霜 / 冰冻 / 闪避 / 敏捷属性 / 生命 / 移速 / 命中
   - 剔除：暴击、暴击伤害、偏斜（偏斜是第二天为了迷雾加的生存）、
     以及只在异界阶段才有意义的点
   - 判据用节点的 `stats` 文本 + `tree05.json` 里的 `stringId` 前缀

3. **保证连通性**（这一步最关键，也是最容易出错的地方）
   游戏要求已点节点必须从职业起点连通。所以：
   - 从锐眼起点（节点 50459）在 `tree05.json` 的 `connections` 图上做 BFS
   - 保留「到起点路径上、且在保留集合里」的节点
   - 被剔除节点的路径要么绕开，要么连带剔除

4. **核对点数**
   - 开荒树（第一天，约 70~75 级）：**70~85 点** 左右
   - 第二天树（92 级）：`passives` 里的节点数应≈等级+任务点（约 110~120）
   - 两边差多少点，就是第二天新加的部分——数量上应该能对上「暴击圈 + 偏斜圈」

5. **产出 + 部署**
   - 写 `POE2-Author-Tree-D1.build` / `POE2-Author-Tree-D2.build`（含中文文件名副本）
   - 网站上「阶段 5 进异界」嵌 D1，「阶段 6 转暴击」嵌 D2
   - 回读验证：两个文件的 `passives` 每个 ID 都在树数据里、都连通、点数合理
   - 给用户一份确认报告（多少节点、哪些大节点、和原树的差集）

## 风险与限制（要说清楚）

- 这是**推出来的**开荒树，不是他原版。他的原版第一天树在那 5 张截图里，
  但图里的小节点只有 3~4 像素，认不出来（详见 `天赋树提取-进展与结论.md`）。
- 如果做完发现点数对不上（比如推出来只有 60 点，而第一天应该有 75 点），
  那就说明他的开荒树里有相当一部分点在第二天被洗掉了——这时候我会
  如实报告，并把 D1 标成「对齐到 75 点的合理版本」，不冒充满分还原。
