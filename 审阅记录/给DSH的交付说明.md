# 给DSH：这次不是只提建议，已交付可运行的修订站

## 接手方式

将包内“网站”目录整体作为新的静态站点内容。根入口仍为 `index.html`；旧版11个主章节hash（start / route / build / skills / tree / craft / solo / combat / trap / dl / faq）全部保留，新增 glossary / sources。不要只替换旧脚本中某几个函数后混用两套导航。

主方案仍是04 Crit Hybrid、保留狙击。没有改动六份Fubgun原件中的节点、连接和装备记录。原始文件字节、原始顺序及数值都有比对；06改用 `.build.txt` 隔离扩展名，内部原字节仍不改。

## 核心信息架构

首页：当前阶段 → 三项行动 → 当前模式下的装备安排 → 下一页。

优先章节：阶段路线、技能、装备、天赋/武器组、实战排障。

进阶章节：打造/涂膏、独狼/交易、机制陷阱、新手问答。

工具章节：全站搜索、中英/ID词典、原始构筑下载、来源与修订。

默认只看04。切换参考分支是查阅行为，不代表角色已完成转型。阶段选择只更新行动建议，不偷偷替换天赋或原始技能数据。

## 内容维护原则

原件原文、作者正文、数据库条目、DSH词典和编辑建议分开归属。不要把作者网页评论当作者本人建议，也不要把默认Early技能表当04快照。

“源文件没有”不等于“作者正文没有”：辅助孔/品质顺序、涂膏、符文和打造分支已补入可核验部分。

中文名按简体资料与ID对照；仍未完成腾讯客户端全量核验。265种天赋映射中，带来源和DSH待复核两种状态必须保留，不能把一个网页重构当作天赋全验证。

## 必须保持的事实修正

- 31级开始冰射不等于龙卷和全部最终技能同时可用；龙卷的原件显示区间从41级开始。
- 原件的技能/装备有显示区间；本批天赋节点没有逐级区间。不要写“所有导出没有level_interval”。
- 虹吸印记II按物理攻击击中伤害偷取，不按全部冰伤计算。
- 弹幕没有狂怒球也有基础额外重复；充能印记和永续充能不负责产球。
- 04没有战斗狂怒、冰尖箭矢和魔力残片，不能混入最高配连接；也没有猛兽之肤/飞羽流矢对应节点。
- 06仅证明文件头与升华节点冲突，不证明作者实际转职，不可擅自改头“修好”。
- 以角色总护盾代替头盔物品护盾、把原件示例词条当最低门槛、把药剂/咒符写成黄装打造，都已纠正。
- 强效寻觅精华与普通寻觅精华区分；Perfect Transmutation是完美蜕变石，不是点金；Greater Exalted是高级崇高石。
- 撤掉无状态前提的工艺成功概率、Vaal Infuser与腐化宝珠的未经核实等同、未固定+3项链可安全剥词条等说法。
- 练级技能简体对应名为“闪电箭矢 / Lightning Arrow”“引雷针 / Lightning Rod”；俗称可作为别名搜索，不代替查询名。

## 构建与测试

编辑 `_source/content.html`、`_source/app.js`、`_source/style.css` 后运行：

```sh
python3 _source/build.py
```

生成器仅用Python标准库，相对路径，不再依赖整理者电脑的绝对路径。数据修改须同步 `_source/data.json` 与 `数据/` 下的导出表。

测试使用Chromium与Playwright，检查157项结构/交互，另有移动键盘焦点检查和截图；详见JSON。运行环境禁止file://与localhost导航，所以本次用真实HTML内存渲染验证交互，不能据此写“国服客户端已导入”或“GitHub线上已发布”。本地下载检查为实际路径和原字节检查；本地存储分别测试禁用降级与内存适配器重初始化。

## 发布前的现场验收

在你的实际部署环境打开首页、返回首页、浏览器前进/后退、深锚点、手机菜单与搜索；逐个试下载04和CSV。检查当前部署路径是否与相对链接一致；确认没有把整个外层审阅包当网站根目录上传。

线上发布以及国服游戏内验证，本次都未执行。
