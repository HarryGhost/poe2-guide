# R5 全站交付检查

核对日期：2026-09-25

## 交付范围

本次不再只改第二章样页。6个剧情阶段、6个异界分支均使用同一个紧凑作业模板。
8个资料/工具页面、完整展开页和首页也采用同一C配色与导航。
共22个HTML文件（其中index与L03同内容）；全部入口不受等级、勾选或登录限制。

保留108组主技能记录、327条内嵌主动/辅助记录；原六份异界文件逐字节保留。
13份属性参考逐份注明底材、基础宝石等级与辅助需求，包含装备而不是只算辅助。
06因冲突不构造虚假的属性与实战结论。

## 检查结果

| 类别 | 通过 / 检查项 | 范围 |
|---|---:|---|
| 静态与数据 | 214 / 214 | 本地链接、锚点、全部阶段入口、ID唯一性、技能辅助顺序、天赋记录、原件哈希、翻译数值 |
| 浏览器与交互 | 1209 / 1209 | 22页面×6宽度、完整技能可见、13属性参考、输入校验、按键同步、144有序分支对比、资源、搜索、无脚本完整页 |
| 附加交付检查 | 58 / 58 | 12构筑副本原样保留、17组文字色对比、无字体文件、交付源码干净重建与字节比对 |
| 总计 | 1481 / 1481 | 各项结果原始JSON同目录保留 |

窗口宽度：1280、1366、1440、1920、768、390像素；桌面优先，窄屏作为回归。
22页未发现整页横向溢出；需要横向查看的大表格保留表内滚动，不裁掉内容。
3张整站对照图覆盖所有剧情、异界和资料工具首屏；关键阶段原尺寸截图同目录可看。

## 本次实际修正

- 从单一样页改为12份完整的阶段作业，不再点击其他阶段回到旧版界面。
- 连接、手法、当前属性使用同一阶段数据；05不再使用04的狙击操作。
- 前两个闪电阶段不再把后期狙击/冰射开孔次序当作当前任务。
- 去除天赋优先级中的重复描述，详细计算明细改为同页展开，降低页面长度。
- 生命保留和精魂分开；空值不再按0放过，避免假的“资源足够”。
- 可选寒霜爆与其属性需求联动；额外变形工具未纳入参考时，明确提示参考失效。
- 改键位同时更新连接和操作；拒绝空键位、重复键位；剪贴板不可用时提供文本回退。
- 06原始和中文提示文件均用.build.txt隔离，原数据不擅自修复。
- 所有源文件、中文术语和长说明入口保留；原文评论不当成作者要求。

## 测试方式与不能冒充的验证

本环境限制Chromium直接打开file://与localhost页面。
浏览器测试实际使用page.set_content载入交付HTML，并内联相同的本地CSS/JS；未改业务脚本。
本地文件链接、锚点和文件字节另以文件系统检查；不是假称真实点击下载已在部署环境成功。
存储恢复测试使用明示的存储适配器检验序列化/恢复逻辑；另测存储不可用时回退。
这不等于已经验收用户真实浏览器的持久化权限。发布后应再查真实打开、下载、存储与复制权限。

这些检查没有发现仍未修复的被测网页问题，但不能保证任意浏览器/游戏环境永远零问题。
国服游戏未登录实测；没有据网页测试宣布实际用点、导入、击杀或装备掉落已验证。

## 保留的数据边界

1. 60+剧情为已核对42–59配置的延续通关方案，不是作者独立60+原件。
2. 完整静态天赋连线与合法逐级点序未取得；节点表不冒充逐点教程。
3. 部分DSH中文映射、国服掉落与客户端名称仍待核验，相关位置保留状态。
4. 本次属性为明确参考配置的完整核算，不是作者未导出的实装宝石等级/珠宝/第二武器扫描。

## 文件完整性

根目录SHA256SUMS.txt列出交付文件哈希（不包含清单自身）。
最终ZIP另经过CRC检测、路径安全检查、解压与逐文件哈希回读；ZIP自身哈希不写入包内以避免循环。
