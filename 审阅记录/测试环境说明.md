# 测试范围与环境

本次生成文件经过157项结构/交互检查，结果157通过、0失败；另检查手机菜单和搜索的键盘焦点，截图可读性由渲染结果复核。4种宽度（320、390、768、1440像素）×13章节均无整页横向溢出，宽表格在自己的容器内滚动。

**限制不能省略：**当前运行环境阻止file://和localhost导航，所以用Chromium直接加载实际HTML的内存文档验证脚本、样式与交互。没有把它称为真实HTTP部署或用户电脑双击文件的端到端验收。相对下载路径逐个检查实际文件与字节；复制按钮测试成功/降级反馈；本地存储测试禁用降级及内存适配器重初始化，未声称完成跨设备同步。

运行复测：安装Playwright和BeautifulSoup4，准备Chromium（可通过环境变量CHROMIUM_PATH指定），在保持包内目录关系的情况下运行 `python3 审阅与测试/运行自动检查.py`。使用的是交付目录和原始备份，未写死整理者目录。Python生成网页本身不依赖这些测试库。

自动检查结果不代表全部游戏机制、国服客户端名称、实际天赋点数或终局首领通关已通过。网页软件测试与游戏方案验收始终分开。
