# R8 修改与验收说明

## 本次修正
**阻断级内容错误已修正**：原先四个异界页面的下一阶段检查项错位，现在从专用fromStage→toStage数据读取。生成器、阶段页和路线总览一起修改，不是只手改首页。

**参考配置已绑定**：完整属性数字、参考装备、技能基础宝石等级同源。Early可以在过渡底材/技能14级与作者展示底材/技能18级之间切换；两套不是同一套实际装备。手动修改需求时明显标注，不把个人录入当作者数据。

**教学案例已添加**：6个实例分别写清状态、词缀槽位、材料分支、做完判断、停手条件。沿用Fubgun阶段与材料依据；示例明确不是作者实装或随机模拟。原12部位教程及8条工艺未删除。

**天赋核验有推进，但没有虚构完整图**：猛兽之肤、诡计面纱、疲乏、驾驭元素的中文/ID/效果与来源交叉核对；只在当前原始节点存在时显示，并支持跳到该行。未获得完整拓扑数据，故没有逐级连线路线。

## 检查方式
- 全部原始及中文提示构筑文件与R7逐字节比对；原技能连接、节点ID/顺序/武器组和8条高级工艺保留。
- 检查26个HTML的唯一ID、本地链接、锚点与C布局。
- 对13个整套参考检查属性总值与装备/宝石同步，覆盖空值、编辑、恢复、可选寒霜爆、键位、复制降级、搜索、资源、关键节点定位、打造切换和算术边界。
- 26页分别检查1366、1920、960、390像素宽度；窄表允许容器内滚动，不出现整页横向溢出。
- 部分代表页另作截图人工查看。并非将“全部页面宽度检查”说成逐页游戏内容核实。

## 环境限制
尝试使用本地HTTP真实加载，Chromium返回ERR_BLOCKED_BY_ADMINISTRATOR。后续使用原HTML和相同本地脚本/样式字节内存载入；没有禁用浏览器安全策略。该方式可以验证渲染、计算、控件和存储被拒的降级，不足以证明GitHub Pages真实导航、跨会话存储、实际剪贴板权限均正常。也没有改动用户线上仓库。

## 硬边界仍保留
完整天赋连线/逐级点序，60+作者独立配置，国服实际导入和名称全量核验，高投入工艺未确认前提与首领实测，均不能因网页测试通过而标记为完成。

## 文件
`静态与原件回归检查.json`、`浏览器交互回归检查.json`、`页面宽度与渲染检查.json`保存逐项结果；`源码重建检查.json`记录生成结果是否可复现。压缩包CRC与逐文件回读另在最终打包时核验。
