# 交付范围与检查结果

版本：六分支进阶版 v2.0；日期：2026-09-24。

本版包含六份原始配置的全部记录：91条装备、60组主技能、238条内嵌技能/辅助、969条天赋记录。记录数不是实际消耗点数。

## 本次已做

01—05进阶总览、06单独隔离；全站统一当前BD；长期目标与比较对象独立；全部六份技能、装备、天赋、资源录入、操作说明和下载完整切换。
增加30组有向差异，并区分原件事实、作者建议、编辑建议和待核对映射。
精魂不再固定150；不同阶段的实际输入分别保留。

## 程序与视觉检查

自动检查554项通过，0项失败。覆盖源文件字节、JSON、中文提示结构、装备数值、全部差异、下载路径、DOM、交互、资源录入与XSS输入处理。
四种宽度为320/390/768/1440；每种宽度检查六个分支的十一个章节，共264个页面布局组合。未发现整页横向溢出，宽表格在自身区域滚动。
六张桌面与手机全页截图在“截图”目录，人工检查了首页、阶段差异、技能排版及隔离提示。截图中的长期目标可能为05，这是演示状态，不是新访客默认。

环境为Chromium内存渲染实际输出HTML；Python本地HTTP另外回读文件字节。没有宣称浏览器导航本地文件、公网部署、真实浏览器重启持久化或国服游戏验收通过。

## 仍未完成，不称已验收

完整天赋坐标连线和合法逐级点序、全量国服客户端名称、实际计点与导入、每个分支的全部终局能力、源文件没有提供的珠宝实装与整套面板。
这些项目在网页对应位置明示。原文件没有被去重、自动修复或拼成虚假的毕业角色。

## 文件检查

各原始.build与用户ZIP逐字节一致。SHA256SUMS.txt记录全部打包文件（不包含该清单本身）。最终压缩包另经CRC和解压回读比较；包装结果见“压缩包检验结果.json”。
