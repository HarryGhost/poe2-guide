# R9 修改与验收

2026-09-26。输入为用户当前R8整合包；本次集中重做打造入口，其他阶段数据不做配装变更。

## 内容改动
20条路线从Early开始；每步有装备状态前提、具体操作、结果判断、异常停手、材料解释和来源。原八条Fubgun工艺、原Early说明及六个实例独立保留，没有使用lowK配方。

普通基础加工不是“作者对每个部位给出的确定配方”；页面标为Fubgun装备目标＋本站操作解释。未验证的高投入环节仍只读，原文公开，不承诺必成。

## 已执行的检查
- 新增操作页静态与浏览器检查：1,176项通过。
- 原阶段页面、属性联动、技能/键位、对比、实例、原打造参考页面回归：339项通过。
- 20条路线的121个步骤均有正文与完整静态副本；材料弹窗、复制降级、异常停手、无效书签、打印和六种窗口宽度逐项检查。
- 28个HTML的本地资源与锚点、全部12个阶段入口及R9页脚检查通过。
- 30份原始构筑/数据/原文与R8逐字节一致。网页清单及新操作数据另行更新。
- 源码独立重建与最终压缩包回读记录另见JSON。

检查结果详见`R9逐项检查.json`、`R9阶段与参考页回归.json`、`原始构筑与数据保留检查.json`。

## 没有执行的验证
浏览器通过内存加载生成HTML，内联同一份本地JS/CSS；没有伪造浏览器存储，也没有宣称真实在线部署已验收。环境限制使直接file://与本机HTTP未作为测试通过项。跨会话阅读位置、实际发布缓存、客户端菜单及国服打造结果均未做实测。

因此这些检查证明的是资料结构与网页行为，不是游戏配方的100%成功率，也不是全部内容零错误保证。
